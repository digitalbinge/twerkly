<%= form_with(model: twerk, local: true) do |form| %>
  <% if twerk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twerk.errors.count, "error") %> prohibited this twerk from being saved:</h2>

      <ul>
      <% twerk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :twerk_content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :twerk_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
